import { CapitalizadoPersonalizadoPipe } from './capitalizado-personalizado.pipe';

describe('CapitalizadoPersonalizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizadoPersonalizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
